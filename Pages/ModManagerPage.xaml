<UserControl
	x:Class="MusicalNoteLauncher.Pages.ModManagerPage"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	Background="{StaticResource BackgroundBrush}">

	<UserControl.Resources>
		<!-- 通用卡片式按钮：使用全局 Surface + 悬浮/按下效果 -->
		<Style x:Key="CardButtonStyle" TargetType="{x:Type Button}">
			<Setter Property="Cursor" Value="Hand" />
			<Setter Property="FontFamily" Value="Microsoft YaHei UI" />
			<Setter Property="Foreground" Value="#FFFFFF" />
			<Setter Property="FontSize" Value="13" />
			<Setter Property="Background" Value="#383838" />
			<Setter Property="BorderBrush" Value="Transparent" />
			<Setter Property="BorderThickness" Value="1" />
			<Setter Property="Padding" Value="14,8" />
			<Setter Property="Template">
				<Setter.Value>
					<ControlTemplate TargetType="{x:Type Button}">
						<Border
							Name="border"
							Background="{TemplateBinding Background}"
							BorderBrush="{TemplateBinding BorderBrush}"
							BorderThickness="{TemplateBinding BorderThickness}"
							CornerRadius="8"
							Padding="{TemplateBinding Padding}"
							SnapsToDevicePixels="True">
							<ContentPresenter
								HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
								VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
						</Border>
						<ControlTemplate.Triggers>
							<Trigger Property="UIElement.IsMouseOver" Value="True">
								<Setter TargetName="border" Property="Background" Value="#444444" />
								<Setter TargetName="border" Property="BorderBrush" Value="#2196F3" />
							</Trigger>
							<Trigger Property="ButtonBase.IsPressed" Value="True">
								<Setter TargetName="border" Property="Background" Value="#2E2E2E" />
							</Trigger>
						</ControlTemplate.Triggers>
					</ControlTemplate>
				</Setter.Value>
			</Setter>
		</Style>

		<!-- 蓝色主要按钮 -->
		<Style x:Key="PrimaryButtonStyle" TargetType="{x:Type Button}" BasedOn="{StaticResource CardButtonStyle}">
			<Setter Property="Background" Value="#2196F3" />
			<Setter Property="Template">
				<Setter.Value>
					<ControlTemplate TargetType="{x:Type Button}">
						<Border
							Name="border"
							Background="{TemplateBinding Background}"
							CornerRadius="8"
							Padding="{TemplateBinding Padding}">
							<ContentPresenter
								HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
								VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
						</Border>
						<ControlTemplate.Triggers>
							<Trigger Property="UIElement.IsMouseOver" Value="True">
								<Setter TargetName="border" Property="Background" Value="#42A5F5" />
							</Trigger>
							<Trigger Property="ButtonBase.IsPressed" Value="True">
								<Setter TargetName="border" Property="Background" Value="#1976D2" />
							</Trigger>
						</ControlTemplate.Triggers>
					</ControlTemplate>
				</Setter.Value>
			</Setter>
		</Style>

		<!-- 绿色成功按钮 -->
		<Style x:Key="SuccessButtonStyle" TargetType="{x:Type Button}" BasedOn="{StaticResource CardButtonStyle}">
			<Setter Property="Background" Value="#2ECC71" />
			<Setter Property="Template">
				<Setter.Value>
					<ControlTemplate TargetType="{x:Type Button}">
						<Border Name="border" Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
							<ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
						</Border>
						<ControlTemplate.Triggers>
							<Trigger Property="UIElement.IsMouseOver" Value="True">
								<Setter TargetName="border" Property="Background" Value="#27AE60" />
							</Trigger>
							<Trigger Property="ButtonBase.IsPressed" Value="True">
								<Setter TargetName="border" Property="Background" Value="#1E8449" />
							</Trigger>
						</ControlTemplate.Triggers>
					</ControlTemplate>
				</Setter.Value>
			</Setter>
		</Style>

		<!-- 红色危险按钮 -->
		<Style x:Key="DangerButtonStyle" TargetType="{x:Type Button}" BasedOn="{StaticResource CardButtonStyle}">
			<Setter Property="Background" Value="#E74C3C" />
			<Setter Property="Template">
				<Setter.Value>
					<ControlTemplate TargetType="{x:Type Button}">
						<Border Name="border" Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
							<ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
						</Border>
						<ControlTemplate.Triggers>
							<Trigger Property="UIElement.IsMouseOver" Value="True">
								<Setter TargetName="border" Property="Background" Value="#C0392B" />
							</Trigger>
							<Trigger Property="ButtonBase.IsPressed" Value="True">
								<Setter TargetName="border" Property="Background" Value="#A93226" />
							</Trigger>
						</ControlTemplate.Triggers>
					</ControlTemplate>
				</Setter.Value>
			</Setter>
		</Style>

	</UserControl.Resources>

	<Grid Background="#1E1E1E">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="*" />
		</Grid.RowDefinitions>

		<!-- ========== 顶部标题栏 ========== -->
		<Border Grid.Row="0" Background="#2D2D2D" Padding="24,18">
			<Grid>
				<StackPanel VerticalAlignment="Center" Orientation="Horizontal">
					<Border Background="#2196F3" CornerRadius="4" Width="4" Height="26" VerticalAlignment="Center" Margin="0,0,14,0" />
					<StackPanel VerticalAlignment="Center">
						<TextBlock Text="模组管理" FontSize="20" FontWeight="Bold" Foreground="#FFFFFF" FontFamily="Microsoft YaHei UI" />
						<TextBlock Name="txtLoaderInfo" FontSize="12" Foreground="#AAAAAA" Margin="0,4,0,0" FontFamily="Microsoft YaHei UI" />
					</StackPanel>
				</StackPanel>
				<StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
					<Button Name="btnRefresh" Content="🔄 刷新" Style="{StaticResource CardButtonStyle}" Margin="0,0,10,0" Click="BtnRefresh_Click" Padding="14,8" />
					<Button Name="btnChangeLoader" Content="安装 / 切换加载器" Style="{StaticResource PrimaryButtonStyle}" Padding="14,8" Click="BtnOpenLoaderSelection_Click" />
				</StackPanel>
			</Grid>
		</Border>

		<!-- ========== 无加载器界面 ========== -->
		<Grid Name="gridNoLoader" Row="1" Visibility="Collapsed">
			<ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Padding="30,24">
				<StackPanel HorizontalAlignment="Center" MaxWidth="820">
					<!-- 提示卡片 -->
					<Border Background="#2D2D2D" CornerRadius="10" Padding="30,28" Margin="0,0,0,24" BorderBrush="#3A3A3A" BorderThickness="1">
						<StackPanel>
							<TextBlock Text="⚠️" FontSize="36" HorizontalAlignment="Center" Margin="0,0,0,10" />
							<TextBlock Text="还未安装任何模组加载器" FontSize="18" FontWeight="SemiBold" Foreground="#FFFFFF" HorizontalAlignment="Center" FontFamily="Microsoft YaHei UI" />
							<TextBlock Name="txtHintVersion" FontSize="13" Foreground="#AAAAAA" HorizontalAlignment="Center" Margin="0,10,0,0" TextWrapping="Wrap" FontFamily="Microsoft YaHei UI" />
						</StackPanel>
					</Border>

					<TextBlock Text="选择要安装的模组加载器" FontSize="14" FontWeight="SemiBold" Foreground="#FFFFFF" Margin="4,0,0,12" FontFamily="Microsoft YaHei UI" />

					<UniformGrid Rows="2" Columns="2">
						<!-- Forge -->
						<Button Margin="0,0,8,12" Tag="Forge" Click="BtnInstallSpecificLoader_Click" HorizontalContentAlignment="Left" VerticalContentAlignment="Stretch" Padding="0" BorderThickness="0" Cursor="Hand" Background="#2D2D2D">
							<Button.Template>
								<ControlTemplate TargetType="{x:Type Button}">
									<Border Name="border" Background="#2D2D2D" CornerRadius="10" BorderBrush="#3A3A3A" BorderThickness="1" Padding="18,16">
										<ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center" />
									</Border>
									<ControlTemplate.Triggers>
										<Trigger Property="UIElement.IsMouseOver" Value="True">
											<Setter TargetName="border" Property="Background" Value="#383838" />
											<Setter TargetName="border" Property="BorderBrush" Value="#2196F3" />
										</Trigger>
										<Trigger Property="ButtonBase.IsPressed" Value="True">
											<Setter TargetName="border" Property="Background" Value="#2E2E2E" />
										</Trigger>
									</ControlTemplate.Triggers>
								</ControlTemplate>
							</Button.Template>
							<Grid>
								<Grid.ColumnDefinitions>
									<ColumnDefinition Width="Auto" />
									<ColumnDefinition Width="*" />
								</Grid.ColumnDefinitions>
								<Border Background="#2196F3" CornerRadius="8" Width="42" Height="42" VerticalAlignment="Center" HorizontalAlignment="Center">
									<TextBlock Text="🔧" FontSize="22" HorizontalAlignment="Center" VerticalAlignment="Center" />
								</Border>
								<StackPanel Grid.Column="1" Margin="14,0,0,0" VerticalAlignment="Center">
									<TextBlock Text="Forge" FontSize="16" FontWeight="SemiBold" Foreground="#FFFFFF" FontFamily="Microsoft YaHei UI" />
									<TextBlock Text="最流行的 Mod 加载器，兼容性最好" Foreground="#87CEFA" FontSize="12" Margin="0,4,0,0" FontFamily="Microsoft YaHei UI" TextWrapping="Wrap" />
								</StackPanel>
							</Grid>
						</Button>

						<!-- Fabric -->
						<Button Margin="8,0,0,12" Tag="Fabric" Click="BtnInstallSpecificLoader_Click" HorizontalContentAlignment="Left" Padding="0" BorderThickness="0" Cursor="Hand">
							<Button.Template>
								<ControlTemplate TargetType="{x:Type Button}">
									<Border Name="border" Background="#2D2D2D" CornerRadius="10" BorderBrush="#3A3A3A" BorderThickness="1" Padding="18,16">
										<ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center" />
									</Border>
									<ControlTemplate.Triggers>
										<Trigger Property="UIElement.IsMouseOver" Value="True">
											<Setter TargetName="border" Property="Background" Value="#383838" />
											<Setter TargetName="border" Property="BorderBrush" Value="#2196F3" />
										</Trigger>
										<Trigger Property="ButtonBase.IsPressed" Value="True">
											<Setter TargetName="border" Property="Background" Value="#2E2E2E" />
										</Trigger>
									</ControlTemplate.Triggers>
								</ControlTemplate>
							</Button.Template>
							<Grid>
								<Grid.ColumnDefinitions>
									<ColumnDefinition Width="Auto" />
									<ColumnDefinition Width="*" />
								</Grid.ColumnDefinitions>
								<Border Background="#2ECC71" CornerRadius="8" Width="42" Height="42" VerticalAlignment="Center" HorizontalAlignment="Center">
									<TextBlock Text="🧵" FontSize="22" HorizontalAlignment="Center" VerticalAlignment="Center" />
								</Border>
								<StackPanel Grid.Column="1" Margin="14,0,0,0" VerticalAlignment="Center">
									<TextBlock Text="Fabric" FontSize="16" FontWeight="SemiBold" Foreground="#FFFFFF" FontFamily="Microsoft YaHei UI" />
									<TextBlock Text="轻量级加载器，启动更快，适合现代 Mod" Foreground="#C8E6C9" FontSize="12" Margin="0,4,0,0" FontFamily="Microsoft YaHei UI" TextWrapping="Wrap" />
								</StackPanel>
							</Grid>
						</Button>

						<!-- NeoForge -->
						<Button Margin="0,12,8,0" Tag="NeoForge" Click="BtnInstallSpecificLoader_Click" HorizontalContentAlignment="Left" Padding="0" BorderThickness="0" Cursor="Hand">
							<Button.Template>
								<ControlTemplate TargetType="{x:Type Button}">
									<Border Name="border" Background="#2D2D2D" CornerRadius="10" BorderBrush="#3A3A3A" BorderThickness="1" Padding="18,16">
										<ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center" />
									</Border>
									<ControlTemplate.Triggers>
										<Trigger Property="UIElement.IsMouseOver" Value="True">
											<Setter TargetName="border" Property="Background" Value="#383838" />
											<Setter TargetName="border" Property="BorderBrush" Value="#2196F3" />
										</Trigger>
										<Trigger Property="ButtonBase.IsPressed" Value="True">
											<Setter TargetName="border" Property="Background" Value="#2E2E2E" />
										</Trigger>
									</ControlTemplate.Triggers>
								</ControlTemplate>
							</Button.Template>
							<Grid>
								<Grid.ColumnDefinitions>
									<ColumnDefinition Width="Auto" />
									<ColumnDefinition Width="*" />
								</Grid.ColumnDefinitions>
								<Border Background="#9B59B6" CornerRadius="8" Width="42" Height="42" VerticalAlignment="Center" HorizontalAlignment="Center">
									<TextBlock Text="🆕" FontSize="22" HorizontalAlignment="Center" VerticalAlignment="Center" />
								</Border>
								<StackPanel Grid.Column="1" Margin="14,0,0,0" VerticalAlignment="Center">
									<TextBlock Text="NeoForge" FontSize="16" FontWeight="SemiBold" Foreground="#FFFFFF" FontFamily="Microsoft YaHei UI" />
									<TextBlock Text="Forge 的现代分支，支持最新游戏版本" Foreground="#D2B4DE" FontSize="12" Margin="0,4,0,0" FontFamily="Microsoft YaHei UI" TextWrapping="Wrap" />
								</StackPanel>
							</Grid>
						</Button>

						<!-- OptiFine -->
						<Button Margin="8,12,0,0" Tag="OptiFine" Click="BtnInstallSpecificLoader_Click" HorizontalContentAlignment="Left" Padding="0" BorderThickness="0" Cursor="Hand">
							<Button.Template>
								<ControlTemplate TargetType="{x:Type Button}">
									<Border Name="border" Background="#2D2D2D" CornerRadius="10" BorderBrush="#3A3A3A" BorderThickness="1" Padding="18,16">
										<ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center" />
									</Border>
									<ControlTemplate.Triggers>
										<Trigger Property="UIElement.IsMouseOver" Value="True">
											<Setter TargetName="border" Property="Background" Value="#383838" />
											<Setter TargetName="border" Property="BorderBrush" Value="#2196F3" />
										</Trigger>
										<Trigger Property="ButtonBase.IsPressed" Value="True">
											<Setter TargetName="border" Property="Background" Value="#2E2E2E" />
										</Trigger>
									</ControlTemplate.Triggers>
								</ControlTemplate>
							</Button.Template>
							<Grid>
								<Grid.ColumnDefinitions>
									<ColumnDefinition Width="Auto" />
									<ColumnDefinition Width="*" />
								</Grid.ColumnDefinitions>
								<Border Background="#F39C12" CornerRadius="8" Width="42" Height="42" VerticalAlignment="Center" HorizontalAlignment="Center">
									<TextBlock Text="✨" FontSize="22" HorizontalAlignment="Center" VerticalAlignment="Center" />
								</Border>
								<StackPanel Grid.Column="1" Margin="14,0,0,0" VerticalAlignment="Center">
									<TextBlock Text="OptiFine" FontSize="16" FontWeight="SemiBold" Foreground="#FFFFFF" FontFamily="Microsoft YaHei UI" />
									<TextBlock Text="性能优化与光影支持，提升帧率与画质" Foreground="#F5D7A1" FontSize="12" Margin="0,4,0,0" FontFamily="Microsoft YaHei UI" TextWrapping="Wrap" />
								</StackPanel>
							</Grid>
						</Button>
					</UniformGrid>

					<TextBlock Text="提示：请先在 “游戏版本” 页面选择你希望安装加载器的 Minecraft 版本。" Foreground="#888888" FontSize="12" Margin="4,18,0,0" TextWrapping="Wrap" FontFamily="Microsoft YaHei UI" />
				</StackPanel>
			</ScrollViewer>
		</Grid>

		<!-- ========== 已检测到加载器：Mod 列表界面 ========== -->
		<Grid Name="gridHasLoader" Row="1" Visibility="Collapsed">
			<Grid Margin="24,18">
				<Grid.RowDefinitions>
					<RowDefinition Height="*" />
					<RowDefinition Height="Auto" />
				</Grid.RowDefinitions>

				<!-- Mod 列表卡片 -->
				<Border Grid.Row="0" Background="#2D2D2D" CornerRadius="10" Padding="0" BorderBrush="#3A3A3A" BorderThickness="1">
					<Grid>
						<Grid.RowDefinitions>
							<RowDefinition Height="Auto" />
							<RowDefinition Height="*" />
						</Grid.RowDefinitions>

						<!-- 卡片标题栏 -->
						<Border Grid.Row="0" Padding="18,14">
							<Grid>
								<StackPanel Orientation="Horizontal" VerticalAlignment="Center">
									<TextBlock Name="txtModListTitle" Text="已安装模组" Foreground="#FFFFFF" FontSize="16" FontWeight="SemiBold" VerticalAlignment="Center" FontFamily="Microsoft YaHei UI" />
									<Border Background="#383838" CornerRadius="10" Padding="10,2" Margin="10,0,0,0" VerticalAlignment="Center">
										<TextBlock Name="txtModCountBadge" Text="共 0 个" Foreground="#AAAAAA" FontSize="12" FontFamily="Microsoft YaHei UI" />
									</Border>
								</StackPanel>
								<StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
									<Button Content="启用选中" Style="{StaticResource SuccessButtonStyle}" Margin="0,0,10,0" Click="BtnEnableMods_Click" Padding="14,8" />
									<Button Content="禁用选中" Style="{StaticResource DangerButtonStyle}" Margin="0,0,10,0" Click="BtnDisableMods_Click" Padding="14,8" />
									<Button Content="卸载选中" Style="{StaticResource CardButtonStyle}" Click="BtnUninstallMod_Click" Padding="14,8">
										<Button.BorderBrush>
											<SolidColorBrush Color="#5A1F1F" />
										</Button.BorderBrush>
									</Button>
								</StackPanel>
							</Grid>
						</Border>

						<Grid Grid.Row="1">
							<!-- 列表本体 -->
							<ListBox
								Name="lstMods"
								Background="Transparent"
								BorderThickness="0"
								Foreground="#FFFFFF"
								FontFamily="Microsoft YaHei UI"
								ScrollViewer.HorizontalScrollBarVisibility="Disabled"
								SelectionMode="Extended"
								Padding="12,0,12,12">
								<ItemsControl.ItemContainerStyle>
									<Style TargetType="{x:Type ListBoxItem}">
										<Setter Property="HorizontalContentAlignment" Value="Stretch" />
										<Setter Property="Padding" Value="0" />
										<Setter Property="Margin" Value="0,4" />
										<Setter Property="Template">
											<Setter.Value>
												<ControlTemplate TargetType="ListBoxItem">
													<Border Name="border" Background="Transparent" CornerRadius="8" Padding="0">
														<ContentPresenter HorizontalAlignment="Stretch" VerticalAlignment="Center" />
													</Border>
													<ControlTemplate.Triggers>
														<Trigger Property="IsSelected" Value="True">
															<Setter TargetName="border" Property="Background" Value="#383838" />
														</Trigger>
														<Trigger Property="UIElement.IsMouseOver" Value="True">
															<Setter TargetName="border" Property="Background" Value="#333333" />
														</Trigger>
													</ControlTemplate.Triggers>
												</ControlTemplate>
											</Setter.Value>
										</Setter>
									</Style>
								</ItemsControl.ItemContainerStyle>
								<ItemsControl.ItemTemplate>
									<DataTemplate>
										<Border Background="#383838" CornerRadius="8" Padding="14,12" Margin="2">
											<Grid>
												<Grid.ColumnDefinitions>
													<ColumnDefinition Width="Auto" />
													<ColumnDefinition Width="Auto" />
													<ColumnDefinition Width="*" />
													<ColumnDefinition Width="Auto" />
												</Grid.ColumnDefinitions>
												<!-- 图标方块 -->
												<Border Grid.Column="0" Width="38" Height="38" CornerRadius="8" Background="#444444" HorizontalAlignment="Center" VerticalAlignment="Center">
													<TextBlock Text="📦" FontSize="18" HorizontalAlignment="Center" VerticalAlignment="Center" />
												</Border>
												<!-- 状态圆圈 -->
												<Ellipse Grid.Column="1" Width="9" Height="9" VerticalAlignment="Center" Margin="12,0,0,0" Fill="#2ECC71">
													<Ellipse.Style>
														<Style TargetType="Ellipse">
															<Setter Property="Fill" Value="#2ECC71" />
															<Style.Triggers>
																<DataTrigger Binding="{Binding IsEnabled}" Value="False">
																	<Setter Property="Fill" Value="#E74C3C" />
																</DataTrigger>
															</Style.Triggers>
														</Style>
													</Ellipse.Style>
												</Ellipse>
												<!-- 名称 + 版本 -->
												<StackPanel Grid.Column="2" Margin="12,0,12,0" VerticalAlignment="Center">
													<TextBlock Foreground="#FFFFFF" FontSize="14" FontWeight="SemiBold" Text="{Binding DisplayName}" TextTrimming="CharacterEllipsis" FontFamily="Microsoft YaHei UI" />
													<TextBlock Foreground="#AAAAAA" FontSize="12" Text="{Binding ModVersion}" Margin="0,3,0,0" FontFamily="Microsoft YaHei UI" />
												</StackPanel>
												<!-- 状态徽章 -->
												<Border Grid.Column="3" Name="statusBadge" CornerRadius="8" Padding="10,4" VerticalAlignment="Center">
													<Border.Style>
														<Style TargetType="Border">
															<Setter Property="Background" Value="#2ECC71" />
															<Style.Triggers>
																<DataTrigger Binding="{Binding IsEnabled}" Value="False">
																	<Setter Property="Background" Value="#E74C3C" />
																</DataTrigger>
															</Style.Triggers>
														</Style>
													</Border.Style>
													<TextBlock Text="{Binding StatusText}" Foreground="#FFFFFF" FontSize="12" FontWeight="SemiBold" FontFamily="Microsoft YaHei UI" />
												</Border>
											</Grid>
										</Border>
									</DataTemplate>
								</ItemsControl.ItemTemplate>
							</ListBox>

							<!-- 空状态 -->
							<Border Name="panelEmptyMods" Background="Transparent" CornerRadius="8" Padding="30" Visibility="Collapsed">
								<StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
									<TextBlock Text="📦" FontSize="48" HorizontalAlignment="Center" Margin="0,0,0,10" />
									<TextBlock Text="暂无模组" FontSize="16" FontWeight="SemiBold" Foreground="#FFFFFF" HorizontalAlignment="Center" FontFamily="Microsoft YaHei UI" />
									<TextBlock Name="txtEmptyModsHint" FontSize="12" Foreground="#AAAAAA" HorizontalAlignment="Center" Margin="0,8,0,0" TextWrapping="Wrap" FontFamily="Microsoft YaHei UI" />
								</StackPanel>
							</Border>
						</Grid>
					</Grid>
				</Border>

				<!-- 底部操作卡片 -->
				<Grid Grid.Row="1" Margin="0,16,0,0">
					<Grid.ColumnDefinitions>
						<ColumnDefinition Width="*" />
						<ColumnDefinition Width="*" />
						<ColumnDefinition Width="*" />
					</Grid.ColumnDefinitions>

					<Button Grid.Column="0" Margin="0,0,8,0" Style="{StaticResource CardButtonStyle}" HorizontalContentAlignment="Left" Click="BtnRefreshMods_Click" Padding="0">
						<Button.Content>
							<StackPanel Margin="16,14" Orientation="Horizontal">
								<Border Background="#2196F3" CornerRadius="8" Width="36" Height="36" VerticalAlignment="Center">
									<TextBlock Text="🔄" FontSize="18" HorizontalAlignment="Center" VerticalAlignment="Center" />
								</Border>
								<StackPanel Margin="12,0,0,0" VerticalAlignment="Center">
									<TextBlock Text="刷新模组" FontSize="14" FontWeight="SemiBold" Foreground="#FFFFFF" FontFamily="Microsoft YaHei UI" />
									<TextBlock Text="重新扫描 mods 目录" FontSize="11" Foreground="#AAAAAA" Margin="0,3,0,0" FontFamily="Microsoft YaHei UI" />
								</StackPanel>
							</StackPanel>
						</Button.Content>
					</Button>

					<Button Grid.Column="1" Margin="8,0,8,0" Style="{StaticResource CardButtonStyle}" HorizontalContentAlignment="Left" Click="BtnAddMod_Click" Padding="0">
						<Button.Content>
							<StackPanel Margin="16,14" Orientation="Horizontal">
								<Border Background="#2ECC71" CornerRadius="8" Width="36" Height="36" VerticalAlignment="Center">
									<TextBlock Text="＋" FontSize="22" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center" />
								</Border>
								<StackPanel Margin="12,0,0,0" VerticalAlignment="Center">
									<TextBlock Text="添加模组" FontSize="14" FontWeight="SemiBold" Foreground="#FFFFFF" FontFamily="Microsoft YaHei UI" />
									<TextBlock Text="选择 .jar 文件添加" FontSize="11" Foreground="#AAAAAA" Margin="0,3,0,0" FontFamily="Microsoft YaHei UI" />
								</StackPanel>
							</StackPanel>
						</Button.Content>
					</Button>

					<Button Grid.Column="2" Margin="8,0,0,0" Style="{StaticResource CardButtonStyle}" HorizontalContentAlignment="Left" Click="BtnModMarket_Click" Padding="0">
						<Button.Content>
							<StackPanel Margin="16,14" Orientation="Horizontal">
								<Border Background="#9B59B6" CornerRadius="8" Width="36" Height="36" VerticalAlignment="Center">
									<TextBlock Text="🛒" FontSize="18" HorizontalAlignment="Center" VerticalAlignment="Center" />
								</Border>
								<StackPanel Margin="12,0,0,0" VerticalAlignment="Center">
									<TextBlock Text="模组下载" FontSize="14" FontWeight="SemiBold" Foreground="#FFFFFF" FontFamily="Microsoft YaHei UI" />
									<TextBlock Text="浏览社区热门 Mod" FontSize="11" Foreground="#AAAAAA" Margin="0,3,0,0" FontFamily="Microsoft YaHei UI" />
								</StackPanel>
							</StackPanel>
						</Button.Content>
					</Button>
				</Grid>
			</Grid>
		</Grid>
	</Grid>
</UserControl>
