<UserControl x:Class="MusicalNoteLauncher.Pages.HomePage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#1E1E1E">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 头部欢迎区域 -->
        <Border Grid.Row="0" Background="#2D2D2D" Padding="20">
            <StackPanel>
                <TextBlock Text="馃幍 欢迎回来！" FontSize="24" Foreground="White" FontFamily="Microsoft YaHei"/>
                <TextBlock Text="准备好开始你的 Minecraft 冒险了吗？" FontSize="14" Foreground="#AAAAAA" Margin="0,5,0,0"/>
            </StackPanel>
        </Border>

        <!-- 主要内容区域 -->
        <ScrollViewer Grid.Row="1" Padding="20" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <!-- 快捷启动卡片 -->
                <Border Background="#2D2D2D" Padding="15" Margin="0,0,0,15">
                    <StackPanel>
                        <TextBlock Text="快䢝启动" FontSize="16" FontWeight="Bold" Foreground="White" Margin="0,0,0,10"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="120"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="100"/>
                            </Grid.ColumnDefinitions>
                            <Border Grid.Column="0" Background="#4CAF50" Height="80"/>
                            <StackPanel Grid.Column="1" Margin="15,0">
                                <TextBlock Text="1.20.1 - 原版" FontSize="14" Foreground="White"/>
                                <TextBlock Text="最新稳定版本" FontSize="12" Foreground="#AAAAAA" Margin="0,3,0,0"/>
                            </StackPanel>
                            <Button Grid.Column="2" Background="#2196F3" Foreground="White" 
                                    BorderThickness="0" Padding="15,10"
                                    FontSize="14" FontWeight="SemiBold">
                                启动
                            </Button>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- 服务器列表 -->
                <Border Background="#2D2D2D" Padding="15" Margin="0,0,0,15">
                    <StackPanel>
                        <TextBlock Text="热门服务器" FontSize="16" FontWeight="Bold" Foreground="White" Margin="0,0,0,10"/>
                        <StackPanel>
                            <Border Background="#383838" Padding="12" Margin="0,0,0,5">
                                <StackPanel Orientation="Horizontal">
                                    <Border Background="#FF9800" Width="8" Height="8" Margin="0,0,10,0"/>
                                    <TextBlock Text="陶瓦联机" FontSize="14" Foreground="White"/>
                                    <TextBlock Text="mc.taowa.cn:25565" FontSize="12" Foreground="#AAAAAA" Margin="10,0,0,0"/>
                                </StackPanel>
                            </Border>
                            <Border Background="#383838" Padding="12" Margin="0,0,0,5">
                                <StackPanel Orientation="Horizontal">
                                    <Border Background="#F44336" Width="8" Height="8" Margin="0,0,10,0"/>
                                    <TextBlock Text="Hypixel" FontSize="14" Foreground="White"/>
                                    <TextBlock Text="mc.hypixel.net" FontSize="12" Foreground="#AAAAAA" Margin="10,0,0,0"/>
                                </StackPanel>
                            </Border>
                            <Border Background="#383838" Padding="12">
                                <StackPanel Orientation="Horizontal">
                                    <Border Background="#9C27B0" Width="8" Height="8" Margin="0,0,10,0"/>
                                    <TextBlock Text="Mineplex" FontSize="14" Foreground="White"/>
                                    <TextBlock Text="us.mineplex.com" FontSize="12" Foreground="#AAAAAA" Margin="10,0,0,0"/>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- 游戏统计 -->
                <Border Background="#2D2D2D" Padding="15">
                    <StackPanel>
                        <TextBlock Text="游戏统计" FontSize="16" FontWeight="Bold" Foreground="White" Margin="0,0,0,10"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Border Grid.Column="0" Background="#383838" Padding="15" Margin="0,0,5,0">
                                <StackPanel>
                                    <TextBlock Text="12" FontSize="28" FontWeight="Bold" Foreground="#4CAF50"/>
                                    <TextBlock Text="已安装版本" FontSize="12" Foreground="#AAAAAA" Margin="0,5,0,0"/>
                                </StackPanel>
                            </Border>
                            <Border Grid.Column="1" Background="#383838" Padding="15" Margin="5,0,5,0">
                                <StackPanel>
                                    <TextBlock Text="256" FontSize="28" FontWeight="Bold" Foreground="#2196F3"/>
                                    <TextBlock Text="模组数量" FontSize="12" Foreground="#AAAAAA" Margin="0,5,0,0"/>
                                </StackPanel>
                            </Border>
                            <Border Grid.Column="2" Background="#383838" Padding="15" Margin="5,0,0,0">
                                <StackPanel>
                                    <TextBlock Text="15" FontSize="28" FontWeight="Bold" Foreground="#FF9800"/>
                                    <TextBlock Text="存档数量" FontSize="12" Foreground="#AAAAAA" Margin="0,5,0,0"/>
                                </StackPanel>
                            </Border>
                        </Grid>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <!-- 底部状态栏 -->
        <Border Grid.Row="2" Background="#2D2D2D" Padding="15">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Java 版本: 21" FontSize="12" Foreground="#AAAAAA"/>
                <TextBlock Text=" | " FontSize="12" Foreground="#444444"/>
                <TextBlock Text="内存: 4GB" FontSize="12" Foreground="#AAAAAA"/>
                <TextBlock Text=" | " FontSize="12" Foreground="#444444"/>
                <TextBlock Text="在线状态" FontSize="12" Foreground="#4CAF50"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>